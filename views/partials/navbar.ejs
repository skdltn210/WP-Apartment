<nav
  class="navbar navbar-expand-lg navbar-custom"
  style="height: 43px; border-bottom: 1px solid #ccc"
>
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="col ms-2">
        <ul class="navbar-nav me-auto ml-1">
          <li class="nav-item">
            <a class="nav-link text-primary" aria-current="page" href="/"
              >Home</a
            >
          </li>
          <% if(currentUser) { %>
          <li class="nav-item">
            <a
              class="nav-link text-primary"
              href="/apartmentData/<%= currentUser._id %>"
              >아파트 정보</a
            >
          </li>
          <li class="nav-item">
            <a
              class="nav-link text-primary"
              href="/userData/<%= currentUser._id %>"
              >유저 정보</a
            >
          </li>
          <% } %>
        </ul>
      </div>
      <div class="col-auto me-1">
        <ul class="navbar-nav ms-auto mr-3">
          <li class="nav-item">
            <% if(currentUser) { %>
            <a class="nav-link logout text-danger" href="/logout" role="button"
              >Logout</a
            >
            <% } else { %>
            <div class="d-flex">
              <a class="nav-link text-primary" href="/login" role="button"
                >Login</a
              >
              <a class="nav-link text-primary" href="/register" role="button"
                >Register</a
              >
            </div>
            <% } %>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
